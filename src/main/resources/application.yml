graphql:
  packages:
    - "com.andreramon.demo"
  subscriptions:
    # Send a ka message every 1000 ms (1 second)
    keepAliveInterval: 1000

spring:
  r2dbc:
    # 'database' represents container name in docker-compose.yml
    url: "r2dbc:postgresql://database/postgres"
    username: "postgres"
    password: "password"